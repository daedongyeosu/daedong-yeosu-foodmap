name: Build Android centered icon v4

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/build-android-icon-v4.yml"
      - ".github/build/android-v4-source.zip.b64"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Restore Android source
        run: |
          base64 -d .github/build/android-v4-source.zip.b64 > /tmp/android-v4.zip
          unzip -q /tmp/android-v4.zip
          for density in mdpi hdpi xhdpi xxhdpi xxxhdpi; do
            mkdir -p "android-app-v4/app/src/main/res/drawable-$density"
            cp android-app-v4/app/src/main/res/mipmap-xxxhdpi/ic_maskable.png "android-app-v4/app/src/main/res/drawable-$density/splash.png"
          done
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - name: Build unsigned release bundle
        working-directory: android-app-v4
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon bundleRelease
      - uses: actions/upload-artifact@v4
        with:
          name: daedong-android-v4-unsigned
          path: android-app-v4/app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
