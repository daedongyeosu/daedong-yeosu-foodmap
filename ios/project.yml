name: DaedongYeosuFoodMap

options:
  bundleIdPrefix: com.daedongmap
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "16.0"

settings:
  base:
    DEVELOPMENT_TEAM: 6LZZ5MWN25
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  DaedongYeosuFoodMap:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: DaedongYeosuFoodMap
        excludes:
          - Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.daedongmap.foodmap
        PRODUCT_NAME: DaedongYeosuFoodMap
        INFOPLIST_FILE: DaedongYeosuFoodMap/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1"
        SUPPORTS_MACCATALYST: NO
        CODE_SIGN_STYLE: Automatic
    scheme:
      testTargets: []
